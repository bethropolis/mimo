show "=== Path + Env Stdlib Test ==="

import path from "path"

import env from "env"

show call path.join("src", "lib", "main.mimo")

show call path.dirname("/tmp/file.txt")

show call path.basename("/tmp/file.txt")

show call path.extname("/tmp/file.txt")

set missing_name "__MIMO_TEST_MISSING_VAR__"

show call env.has(missing_name)

show call env.get(missing_name, "fallback")

set all_vars call env.all()

show type

all_vars
show "=== Path + Env Stdlib Test Complete ==="
