function findMax(arr)
    set max arr[0]
    set i 1
    while < i 5
        if > arr[i] max
            set max arr[i]
        endif
        set i + i 1
    endwhile
    return max
endfunction

set numbers [5, 3, 9, 1, 7]
call findMax(numbers) -> maxNumber
show maxNumber